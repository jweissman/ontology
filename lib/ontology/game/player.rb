# should probably descend from some global actor class?
# the trick is that this model has important security implications, etc.
# eventually needs to communicate with remote DB (or maybe we're authoritative about players?)
class Ontology::Game::Player

end
